# Apps

Primary product applications live here:

- `backend`: FastAPI API and orchestration layer
- `frontend`: React/Vite UI
- `mcp-server`: Go MCP server for Kubernetes and lab diagnostics
- `mcp-ssh-server`: SSH-oriented MCP helper

Keeping these under one root makes the repo easier to read and keeps product code separate from lab assets.
