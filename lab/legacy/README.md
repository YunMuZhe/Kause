# Legacy

This directory keeps older experiment paths that are still useful for reference but are no longer the main product path.

Current contents:

- `qwen-agent`: the original standalone Qwen-Agent integration and HTML helper
